# Data Structure

Planned directories:

- `demo/` — synthetic interview data
- `schemas/` — validated field definitions and units
- `reference/` — approved constants and technical references
- `private/` — local-only real data; never committed

All committed demo data must be synthetic and clearly labelled. Every field must have a defined unit, source, default rule, and validation rule.
